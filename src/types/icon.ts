export type IconPropsType = {
  size: string;
  colorIcon: string;
  colorBg: string;
};

export type IconWithoutBgPropsType = {
  size: string;
  color: string;
};

export type IconEmptyColorType = {
  color: string;
};
